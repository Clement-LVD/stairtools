# Add overall going and rise dimension lines to a stair scene

Add overall going and rise dimension lines to a stair scene

## Usage

``` r
scene_add_dimensions(scene, layout, offset = 15)
```

## Arguments

- scene:

  A `stair_scene`.

- layout:

  A rendered geometry.

- offset:

  Distance, in cm, between the outline and the dimension lines.
