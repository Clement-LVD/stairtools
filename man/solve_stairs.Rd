% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve_stairs.R
\name{solve_stairs}
\alias{solve_stairs}
\title{Compute all possible stair solutions}
\usage{
solve_stairs(
  total_height,
  max_horizontal_run,
  rise_min = 16,
  rise_max = 20,
  rise_target = 16,
  blondel_target = 63,
  show_invalid_solutions = FALSE
)
}
\arguments{
\item{total_height}{\code{numeric} - Total vertical height to climb (cm).}

\item{max_horizontal_run}{\code{numeric} - Maximum available horizontal length (cm).}

\item{rise_min}{\code{numeric} - Minimum acceptable step height (cm). Default: 16.}

\item{rise_max}{\code{numeric} - Maximum acceptable step height (cm). Default: 20.}

\item{rise_target}{\code{numeric} - Target step height used to rank solutions (cm). Default: 16.}

\item{blondel_target}{\code{numeric} - Target value for Blondel's formula \code{2h + g}.
Default: 63 cm.}

\item{show_invalid_solutions}{\code{logical} - If \code{TRUE}, returns all generated
solutions, including solutions that do not satisfy the constraints.
Default: \code{FALSE}.}
}
\value{
A \code{data.frame} containing one row per generated solution.
Geometry is stored in the \code{geometry} list-column.
}
\description{
Main function of the package. Generates all feasible stair geometries
matching the specified constraints without selecting a preferred solution.
}
\details{
For each valid number of steps, possible tread values are generated and
complete stair geometries are computed. All solutions are returned in a
single data frame, with geometry stored as a list-column.
}
\examples{
sol <- solve_stairs(total_height = 160, max_horizontal_run = 150)

sol

plot(sol$geometry[[1]])

# Or get all the solutions, even impossibles
sol2 <- solve_stairs(160, 150, show_invalid_solutions = TRUE)
# plot the best solution :

meilleure <- meilleure_solution(sol2)

# Filter out valid solution
subset(sol, is_valid)
}
