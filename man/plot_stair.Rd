% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_stair_v2.R
\name{plot_stair}
\alias{plot_stair}
\title{Plot a stair geometry}
\usage{
plot_stair(
  geometry,
  riser = TRUE,
  polygon_params = list(col = "white", border = "black"),
  styles = NULL,
  legend_columns = c(`Step begin` = "x_tread_start", `Step end` = "x_tread_end", Riser =
    "x_riser"),
  axis_y_columns = c(`Step height` = "y_top"),
  xlim = NULL,
  ylim = NULL,
  asp = 1
)
}
\arguments{
\item{geometry}{A \code{stair_geometry} data frame.}

\item{riser}{Logical; whether to draw risers. Defaults to \code{TRUE}.}

\item{polygon_params}{Named list of graphical parameters passed to
\code{\link[graphics:polygon]{graphics::polygon()}}. Common parameters include \code{col}, \code{border},
\code{density}, \code{angle}, \code{lty}, and \code{lwd}.}

\item{styles}{List of style rules applied after \code{polygon_params}.
Each rule may contain \code{steps} and \code{surface} selectors. Missing or
\code{NULL} selectors match all polygons. Rules are applied in order.}

\item{legend_columns}{Named character vector of geometry columns to
display as horizontal axes.}

\item{axis_y_columns}{Named character vector of geometry columns to
display as vertical axes.}

\item{xlim}{Optional x-axis limits.}

\item{ylim}{Optional y-axis limits.}

\item{asp}{Plot aspect ratio.}
}
\value{
Invisibly returns the polygon drawing instructions.
}
\description{
Plots a stair geometry using its physical tread and riser surfaces.
Graphical parameters are passed directly to \code{\link[graphics:polygon]{graphics::polygon()}}.
}
\examples{
sol <- solve_stairs(103, 133)
plot_stair(sol$geometry[[1]])

plot_stair(
  sol$geometry[[1]],
  polygon_params = list(col = "white", border = "black"),
  styles = list(
    list(
      steps = 3,
      density = 20,
      angle = 45
    )
  )
)

}
